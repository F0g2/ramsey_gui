// Estado compartilhado do app (global = singleton). Os componentes leem/escrevem
// aqui em vez de passar tudo pela mao pelo painel.
export global Estado {
    in-out property <int> aba: 0;     // 0 Principal · 1 Avançado
    in-out property <int> idioma: 0;  // 0 PT · 1 EN · 2 ES

    out property <[string]> idiomas: ["Português", "English", "Español"];

    // ---- Parametros (aba Principal) ----
    in-out property <int> metodo: 0;     // 0 Enumeração · 1 SAT
    in-out property <int> algoritmo: 0;  // 2o dropdown; opcoes dependem do metodo
    in-out property <int> s: 3;
    in-out property <int> t: 3;
    in-out property <int> n: 4;       // numero de vertices (default pequeno)

    // ---- Avançado ----
    in-out property <bool> sym: true;        // quebra de simetria
    in-out property <int> timeout: 0;        // segundos; 0 = sem limite
    in-out property <int> threads: 4;
    in-out property <int> backend: 0;        // indice em 'backends'
    in-out property <int> verbosidade: 1;    // indice em 'verbosidades'

    out property <[string]> backends: ["CaDiCaL", "Glucose", "MiniSat"];
}
